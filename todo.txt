--fix player display
--make more robust
--add captain symbol for draft



ingame events
--bounty rune breakdown
	-- rewrite for patch
	-- every spawn
	-- overall
--buyback calc
if reliable + (unreliable-(unreliable/40)) > floor(200+(net worth/13))