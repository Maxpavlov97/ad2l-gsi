var d2gsi = require('../index.js');
var server = new d2gsi();

server.events.on('newclient', function(client) {
    console.log("New client connection, IP address: " + client.ip + ", Auth token: " + client.auth);
    console.log(client.gamestate.player.team2.player0);
    
    client.on('player:activity', function(activity) {
        console.log(activity);
    });
    client.on('map:name', function(activity) {
        console.log(activity);
    });
    client.on('hero:level', function(level) {
        console.log("Now level " + level);
    });
    // client.on('draft:activeteam', function(level) {
    //     console.log("draft:activeteam " + level);
    // });
    // console.log(client.eventNames());
    // client.on('draft:activeteam_time_remaining', function(level) {
    //     console.log("draft:activeteam_time_remaining " + level);
    // });
    // client.on('draft:', function(level) {
    //     console.log("draft " + level);
    // });
    // client.on('draft:team3:pick2_id', function(level) {
    //     console.log("draft:team3:pick2_id " + level);
    // });
    // client.on('draft:team3:pick2_class', function(level) {
    //     console.log("draft:team3:pick2_class  " + level);
    // });
    // client.on('draft:team3:home_team', function(level) {
    //     console.log("draft:team3:home_team " + level);
    // });
    // client.on('newdata', function(level) {
    //     console.log(level);
    // });
    // client.on('abilities:ability0:can_cast', function(can_cast) {
    //     if (can_cast) console.log("Ability0 off cooldown!");
    // });

    // draft:activeteam
    // draft:activeteam_time_remaining
    // draft:dire_bonus_time
    // draft:pick
    // draft:radiant_bonus_time
    // draft:team#:home_team
    // draft:team#:ban#_class (0-5)
    // draft:team#:ban#_id (0-5)
    // draft:team#:pick#_class (0-4)
    // draft:team#:pick#_id (0-4)


   
});